<%= form_with(model: course_connection, local: true) do |form| %>
  <% if course_connection.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(course_connection.errors.count, "error") %> prohibited this course_connection from being saved:</h2>

      <ul>
        <% course_connection.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :parent_course_code %>
    <%= form.collection_select(:parent_course_code, Course.all, :code, :select_name) %>
  </div>

  <div class="field">
    <%= form.label :child_course_code %>
    <%= form.collection_select(:child_course_code, Course.all, :code, :select_name) %>
  </div>

  <div class="field">
    <%= form.label :required %>
    <%=  form.select(:required,[['mandatory','mandatory'],['required','required'],['elective','elective']])%>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
